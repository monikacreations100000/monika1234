{
  "name": "monikas-creation-backend",
  "version": "1.0.0",
  "description": "Backend server for Monika's Creation e-commerce platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install --prefix ../frontend && npm run build --prefix ../frontend && mkdir -p public && cp -R ../frontend/dist/. ./public/"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
